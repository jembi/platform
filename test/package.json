{
  "name": "platform-test",
  "version": "1.0.0",
  "description": "A package to test the platform",
  "scripts": {
    "test": "mocha package/**/*"
  },
  "devDependencies": {
    "mocha": "10.0.0"
  },
  "dependencies": {
    "dockerode": "^3.3.2"
  }
}
