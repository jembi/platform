{
  "id": "elastic-monitoring",
  "name": "Elastic Monitoring",
  "description": "Deploy monitoring tools that work with the elastic stack to record Docker container and host logs",
  "type": "infrastructure",
  "version": "v0.2.0",
  "dependencies": [
    "core", "elastic-analytics", "elastic-pipeline"
  ]
}
