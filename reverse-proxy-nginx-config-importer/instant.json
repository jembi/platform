{
  "id": "reverse-proxy-nginx-config-importer",
  "name": "Nginx Reverse Proxy config importer package",
  "description": "nginx reverse proxy config importer",
  "type": "use-case",
  "version": "v0.0.1",
  "dependencies": ["reverse-proxy-nginx"],
  "environmentVariables": {
    "INSECURE":false,
    "INSECURE_PORTS":"-5001:5001-80:80-8080:8080",
    "DOMAIN_NAME":"localhost",
    "RENEWAL_EMAIL":"dummy@jembi.org"
  }
}
